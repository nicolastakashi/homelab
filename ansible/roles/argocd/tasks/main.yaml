- name: Basic usage
  ansible.builtin.debug:
    msg: "'{{ lookup('ansible.builtin.env', 'HOME') }}' is the HOME environment variable."
    
- name: Copy Git Repo Secret
  template:
    src: "homelabsecret.j2"
    dest: "/tmp/homelabsecret.yaml"
    owner: root
    group: root
    mode: 0755